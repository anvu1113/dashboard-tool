version: '3.8'

services:
  laravel:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: dashboard-tool-laravel
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./backend:/var/www/html
      - ./backend/storage:/var/www/html/storage
    ports:
      - "8001:8000"
    environment:
      DB_CONNECTION: mysql
      DB_HOST: host.docker.internal
      DB_PORT: 3306
      DB_DATABASE: dashboard_tool
      DB_USERNAME: root
      DB_PASSWORD: root
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - dashboard-tool-network

  nuxt:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: dashboard-tool-nuxt
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "3001:3001"
    environment:
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3001
      - API_BASE_URL=http://localhost:8001
    depends_on:
      - laravel
    networks:
      - dashboard-tool-network

networks:
  dashboard-tool-network:
    driver: bridge
